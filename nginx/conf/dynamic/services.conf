# AUTO-GENERATED SERVICE DISCOVERY CONFIG
# Generated at: 2025-10-14T12:28:00+05:00
# DO NOT EDIT MANUALLY - Changes will be overwritten


# Service: demo
location /demo/ {
    # Authentication
    auth_request /verify;
    auth_request_set $auth_status $upstream_status;
    auth_request_set $auth_user_name $upstream_http_x_user_name;
    auth_request_set $auth_user_id $upstream_http_x_user_id;
    auth_request_set $auth_user_admin $upstream_http_x_user_admin;
    auth_request_set $auth_user_roles $upstream_http_x_user_roles;
    auth_request_set $auth_user_permissions $upstream_http_x_user_permissions;
    auth_request_set $auth_user_service_roles $upstream_http_x_user_service_roles;
    auth_request_set $auth_user_service_permissions $upstream_http_x_user_service_permissions;
    
    # Strip the service prefix before proxying
    rewrite ^/demo/(.*) /$1 break;
    
    proxy_pass http://demo-service:5000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /demo;
    proxy_set_header X-Original-URI $request_uri;
    
    # Pass authentication information
    proxy_set_header X-User-Name $auth_user_name;
    proxy_set_header X-User-ID $auth_user_id;
    proxy_set_header X-User-Admin $auth_user_admin;
    proxy_set_header X-User-Roles $auth_user_roles;
    proxy_set_header X-User-Permissions $auth_user_permissions;
    proxy_set_header X-User-Service-Roles $auth_user_service_roles;
    proxy_set_header X-User-Service-Permissions $auth_user_service_permissions;
    
    # Add service identification headers
    proxy_set_header X-Service-Key demo;
    proxy_set_header X-Service-Prefix /demo;
    
    # Timeout settings
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    
    # Buffer settings
    proxy_buffering on;
    proxy_buffer_size 4k;
    proxy_buffers 8 4k;
    proxy_busy_buffers_size 8k;
}

# Health check endpoint for demo
location = /demo/health {
    proxy_pass http://demo-service:5000/health;
    proxy_set_header Host $host;
    access_log off;
}


# Service: referal
# Static files for referal service
location /referal/static/ {
    proxy_pass http://referal:80/static/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Caching for static files
    proxy_cache_valid 200 1h;
    proxy_cache_use_stale error timeout invalid_header updating http_500 http_502 http_503 http_504;
    add_header Cache-Control "public, max-age=3600" always;
    expires 1h;
}

location /referal/ {
    # Authentication
    auth_request /verify;
    auth_request_set $auth_status $upstream_status;
    auth_request_set $auth_user_name $upstream_http_x_user_name;
    auth_request_set $auth_user_id $upstream_http_x_user_id;
    auth_request_set $auth_user_admin $upstream_http_x_user_admin;
    auth_request_set $auth_user_roles $upstream_http_x_user_roles;
    auth_request_set $auth_user_permissions $upstream_http_x_user_permissions;
    auth_request_set $auth_user_service_roles $upstream_http_x_user_service_roles;
    auth_request_set $auth_user_service_permissions $upstream_http_x_user_service_permissions;
    
    # Strip the service prefix before proxying
    rewrite ^/referal/(.*) /$1 break;
    
    proxy_pass http://referal:80;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /referal;
    proxy_set_header X-Original-URI $request_uri;
    
    # Pass authentication information
    proxy_set_header X-User-Name $auth_user_name;
    proxy_set_header X-User-ID $auth_user_id;
    proxy_set_header X-User-Admin $auth_user_admin;
    proxy_set_header X-User-Roles $auth_user_roles;
    proxy_set_header X-User-Permissions $auth_user_permissions;
    proxy_set_header X-User-Service-Roles $auth_user_service_roles;
    proxy_set_header X-User-Service-Permissions $auth_user_service_permissions;
    
    # Add service identification headers
    proxy_set_header X-Service-Key referal;
    proxy_set_header X-Service-Prefix /referal;
    
    # Timeout settings
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    
    # Buffer settings
    proxy_buffering on;
    proxy_buffer_size 4k;
    proxy_buffers 8 4k;
    proxy_busy_buffers_size 8k;
}

# Health check endpoint for referal
location = /referal/health {
    proxy_pass http://referal:80/health;
    proxy_set_header Host $host;
    access_log off;
}
