services:
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: gateway-nginx-1
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - auth-service
    volumes:
      # Mount main nginx config
      - ./nginx/conf/nginx.conf:/etc/nginx/nginx.conf:ro
      # Mount conf.d configs
      - ./nginx/conf/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/conf/gateway.inc:/etc/nginx/conf.d/gateway.inc:ro
      - ./nginx/conf/legacy.inc:/etc/nginx/conf.d/legacy.inc:ro
      # Mount dynamic configs (writable for service discovery)
      - nginx_dynamic_config:/etc/nginx/conf.d/dynamic
      # Mount certs
      - ./nginx/certs:/etc/nginx/certs:ro
    environment:
      - TZ=Asia/Tashkent
    networks:
      - public_network
      - auth_network
      - service_network
    restart: always

  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    env_file:
      - ./auth-service/.env
    depends_on:
      - mongo
    networks:
      - auth_network
      - data_network
      - public_network
    volumes:
      - ./auth-service/data:/root/data
      - ./auth-service/static:/root/static
      - ./auth-service/templates:/root/templates
      - nginx_dynamic_config:/etc/nginx/conf.d/dynamic  # Shared volume for nginx configs
      - /var/run/docker.sock:/var/run/docker.sock:ro  # Access to Docker for nginx reload
    environment:
      - TZ=Asia/Tashkent
      - NOTIFICATION_SERVICE_URL=http://notification-service:8082
      - NGINX_CONTAINER_NAME=gateway-nginx-1
      - NGINX_DYNAMIC_CONFIG_DIR=/etc/nginx/conf.d/dynamic
    restart: always

  mongo:
    image: mongo:6
    restart: always
    ports:
      - "27017:27017"  # Возвращаем для разработки
    networks:
      - data_network
      - public_network  # Возвращаем для внешнего доступа
    volumes:
      - ./mongo_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=authdb
      - TZ=Asia/Tashkent

  dozzle:
    image: amir20/dozzle:latest
    container_name: gateway-dozzle
    restart: always
    ports:
      - "9999:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - DOZZLE_LEVEL=info
      - DOZZLE_TAILSIZE=300
      - DOZZLE_FILTER=name=gateway-*,name=referal,name=notification*
      - DOZZLE_NO_ANALYTICS=true
      - DOZZLE_BASE=/logs
      - TZ=Asia/Tashkent
      # Customize theme with Golden House colors
      - DOZZLE_THEME_PRIMARY=#d4af37
      - DOZZLE_THEME_BACKGROUND=#f5f5f5
    networks:
      - public_network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/logs"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  nginx_dynamic_config:  # Shared volume for dynamic nginx configs

networks:
  # Публичная сеть - для nginx и всех сервисов
  public_network:
    name: public_network
    driver: bridge
  
  # Внутренние сети
  auth_network:
    name: auth_network
    driver: bridge
  
  service_network:
    name: service_network
    driver: bridge
  
  data_network:
    name: data_network
    driver: bridge
