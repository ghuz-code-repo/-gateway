// Particles.js configuration
// This file is loaded by the profile page but not actually used
// Created to prevent 404 errors

console.log('particles-config.js loaded');

// Empty configuration - can be expanded later if needed
